const players = "Natalia, Maria, Joane, Ana";

console.log(players.split("")); //retorna uma array com todas as letras separadas
console.log(players.split(", ")); //retorna um array com as palavras separadas pela virgula.

const name = "Agatha Christie Maria";
console.log(name.split(" ", 2)); //retorna um array com as duas primeiras palavras, separadas por espaço.
